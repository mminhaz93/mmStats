import { TOOGLE_DARKMODE } from './types'

export const toggleDarkMode = isDarkMode => ({
  type: TOOGLE_DARKMODE,
  isDarkMode,
})

export const toggleDarkModeExtra = isDarkMode => ({
  type: TOOGLE_DARKMODE,
  isDarkMode,
})
